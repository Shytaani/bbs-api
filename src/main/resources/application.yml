spring:
    dataSource:
        url: jdbc:mysql://localhost:3306/bbs?serverTimezone=JST
        username: foo
        password: bar
    jackson:
        time-zone: Asia/Tokyo
    jpa:
        database: MYSQL
        hibernate:
          ddl-auto: update

server:
    servlet:
        context-path: /bbs/api